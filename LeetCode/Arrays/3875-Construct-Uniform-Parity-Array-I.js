/**
 * Problem: 3875. Construct Uniform Parity Array I
 * Approach: Parity Observation
 *
 * If all numbers have the same parity, we can keep them as they are.
 * If there are both odd and even numbers, subtract a number with
 * opposite parity to get an odd number.
 *
 * Therefore, a valid array can always be constructed.
 *
 * Time Complexity: O(1)
 * Space Complexity: O(1)
 */

/**
 * @param {number[]} nums1
 * @return {boolean}
 */

var uniformArray = function(nums1) {
    return true;
};